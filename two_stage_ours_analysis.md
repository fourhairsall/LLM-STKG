# ours + 两阶段重排 —— 多算例实验结论（Foursquare-NYC 全量）

> 主脚本：`run_two_stage_ours_sweep.py --device cuda --seeds 42,123,777 --variants sem_free,with_bge --K 50 --rerank_epochs 30`
> 阶段1 = 生产 ours（C1 bge + SGCP + **C6 ctx 行为先验** + dot，seed42 权重 `_c6u_seed42.pt`），全候选打分只前向一次
> 阶段2 = 独立小 MLP 重排器，输入 `[q ‖ emb[c] ‖ s1[c]]`（**-Semantics 变体**）/ 额外拼接 768 维 BGE 语义（**+BGE 变体**），分数融合 final = s1 + λ·z（λ=0.3）
> 扫描矩阵：`{语义无关, +BGE语义} × {seed 42,123,777}` = **6 个重排配置**，全部落盘于 `two_stage_ours_sweep.json`

## 一、核心结果（full-candidate, n_test=1447）

| 模型 | R@5 | R@10 | NDCG@10 | 冷启动 R@10* |
|---|---|---|---|---|
| Popularity (Pop) | 0.0270 | 0.0401 | 0.0225 | — |
| BPR-MF | 0.3656 | 0.3884 | 0.3087 | — |
| LightGCN | 0.4029 | 0.5017 | 0.3202 | 0.2643 |
| **LLM-STKG (ours) 生产评估** | **0.5252** | **0.6475** | **0.4208** | **0.5036** |
| Ours stage-1（s1 单独打分） | 0.5252 | 0.6482 | 0.4213 | — |
| Ours + ReRanker(-Semantics) s42 | 0.5245 | 0.6489 | 0.4215 | — |
| Ours + ReRanker(-Semantics) s123 | 0.5245 | 0.6489 | 0.4219 | — |
| Ours + ReRanker(-Semantics) s777 | 0.5245 | 0.6489 | 0.4215 | — |
| **Ours + ReRanker(-Semantics) [3 seeds 聚合]** | 0.5245 | **0.6489±0.0000** | **0.4216±0.0002** | 0.5071 |
| Ours + ReRanker(+BGE) s42 | 0.5252 | 0.6489 | 0.4217 | — |
| Ours + ReRanker(+BGE) s123 | 0.5252 | 0.6489 | 0.4219 | — |
| Ours + ReRanker(+BGE) s777 | 0.5252 | 0.6489 | 0.4216 | — |
| **Ours + ReRanker(+BGE) [3 seeds 聚合]** | 0.5252 | **0.6489±0.0000** | **0.4217±0.0001** | 0.5071 |

\* 冷启动 = 测试目标训练频次 ≤5（n=280），与 `head_to_head_c1_sgcp.json` 定义一致。

## 二、多算例证据：增益是噪声级、且与种子/语义变体无关（关键结论 1）

把单一种子的"0 增益"结论扩展为 **6 个独立算例** 的扫描，结果呈强一致性：

- **跨种子稳定**：语义无关变体 3 个种子的 R@10 完全相同（0.6489，std=0.0），NDCG@10 仅在 0.4215~0.4219 间抖动（std≈0.0002）。+BGE 变体同样 R@10=0.6489（std=0.0）、NDCG@10 0.4216~0.4219。
- **跨变体相同**：+BGE 语义变体与 -Semantics 变体的聚合值 **R@10 完全一致（0.6489）、NDCG@10 仅差 0.0001** → 额外喂入 768 维 BGE 语义向量后，数字没有任何可测量变化。
- **相对基线**：两种重排变体相对"stage-1 s1 单独打分"（0.6482/0.4213）仅 +0.0007 R@10 / +0.0003~0.0004 NDCG；相对"生产 ours"（0.6475/0.4208）仅 +0.0014 R@10 / +0.0006~0.0011 NDCG。

→ **证据闭合**：重排器对已是 SOTA 的 ours 既无噪声之上的收益、也不因换种子或喂语义而出现任何差异。两阶段重排在本项目是**死胡同**，且这一结论现在由多种子多变的对照（而非单点）支撑。

## 三、反证前序 LightGCN 重排"增益"的假象（关键结论 1 补充）

上一轮"重排器把 LightGCN 从 0.4499 拉到 0.4921（+11.4% NDCG）"看起来有效，但：
- 重实现 LGCN 的 stage-1 是 **0.4499**，比官方 LightGCN（0.5017）弱约 5pt —— 即重排器只是**追回一个弱骨干**，并非真正超越单模型。
- 把同一类重排器叠到**真正最强模型 ours（0.6475）**上，增益归零（§二）。
- 因此该路线对强模型"无头可补"，不具备方法论价值，不能构成论文/专利的贡献。

## 四、颠覆性发现：所谓"NDCG 缺口"是旧模型的假象（关键结论 2）

- `head_to_head_c1_sgcp.json` 记录的 ours = R@10 **0.5612 / NDCG@10 0.2558** —— 那是**未启用 C6 行为先验**的旧 ours（prior_channels 为空）。
- 启用 C6（cnt/rec/pop + 上下文门控）后，生产 ours = **R@10 0.6475 / NDCG@10 0.4208**（已用 `u_c6_full_s42.json` 交叉验证，与加载权重完全一致）。
- 因此：**当前生产 ours 在 R@10 与 NDCG 双指标都大幅领先 LightGCN**（+29% / +31%），此前困扰项目的"NDCG 缺口（ours 0.256 < LGCN 0.271）"是**旧模型 artifact，已不存在**。
- 诚实分解：ours 的巨大 R@10/NDCG 主要来自 **C6 行为先验**（本基准 75.7% 测试目标为重访，零参数 HF 规则已达 R@10 0.6275；ours+C6 = 0.6475 ≈ HF + 少量 KG/序列精炼）。**LLM-KG 的独特、可辩护贡献在冷启动**：冷启动 R@10 0.5036 vs LightGCN 0.2643（**+91%**）—— 冷启 POI 无共现信号，CF 哑火，KG 语义/类别/地理才起作用。

## 五、LLM 语义"作为特征"路线第 5 次证伪（关键结论 3，本次在强模型上补全）

六条独立失败证据链闭合：
1. LLM-KG 语义边：加/不加 R@10 0.6440 vs 0.6453（net-zero）
2. LightGCN + BGE 节点 init/resid/freeze：塌缩（0.21~0.32）
3. 6 个 NDCG 杠杆（BPR/ListNet/hard-neg/neg↑/attn 打分头/SGCP）
4. 两阶段重排器 **+BGE 语义特征**（弱 stage-1 LGCN 上）：0 净增益
5. **（本轮，强模型）ours + ReRanker(+BGE) [3 seeds] = 0.6489±0.0000 / 0.4217±0.0001** —— 与 -Semantics 变体**完全相同**，确认即便在最强模型上喂语义也零增益
6. （本轮，强模型）ours + ReRanker(-Semantics) [3 seeds] = 0.6489±0.0000 / 0.4216±0.0002 —— 即便不喂语义，重排器也无增益

→ 在 Foursquare-NYC 上，**BGE/LLM 语义向量与共现/时序信号正交**，任何"把语义塞进 ranking 特征"的做法（含两阶段重排的特征版）都失败。LLM 的真实价值只在**结构化先验（KG 图 / 冷启动）**，不在特征。

## 六、战略结论（给论文/专利）

1. **不要再在"语义作为特征"或"两阶段重排"上投入**——已 6 次证伪、多种子多变体对照确认全是死胡同。
2. **新颖性锚点重新定为**：
   - **(a) C6 上下文门控行为先验**：真实、大增益、假设空间严格包含 HF/HR/Pop（不可系统性劣于平凡基线），可辩护；
   - **(b) 结构化 LLM-KG 先验**：带来**冷启动霸权（+91%）**，这是 CF 绝对做不到的、LLM 唯一被证实的排名贡献；
   - **(c) 全候选 R@10/NDCG 双双超越 SOTA**（0.6475 / 0.4208 vs LGCN 0.5017 / 0.3202）。
3. **"NDCG 缺口"叙事应作废**：C6 启用后 ours 的 NDCG 已高于 LightGCN，硬约束（R@10≥0.481）被大幅超额满足。
4. 论文/专利应诚实写明：本基准 75.7% 重访结构下，C6 行为先验是 R@10/NDCG 主驱动；LLM-KG 的边际排名贡献集中在冷启动与稀疏 POI，这是方法诚实的定位。
